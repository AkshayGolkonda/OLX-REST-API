spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AkshayGolkonda/olx-app-config
          username: AkshayGolkonda
          clone-on-start: true
          default-label: main

server:
  port: 8888